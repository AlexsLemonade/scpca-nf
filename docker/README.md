# Docker images for `scpca-nf`

This directory contains Dockerfiles for images used in the `scpca-nf` workflow.
Each image is defined in a separate directory that should be named with the name of the image that will be created by the Dockerfile in that directory.
