// stub config file for running the pipeline locally

process.executor = 'local'
docker.enabled = false


params{
  run_metafile = '${projectDir}/test/stub-metadata.tsv'
  cellhash_pool_file = '${projectDir}/test/stub-pools.tsv'
  outdir = "${projectDir}/test/output"
  run_ids = "All"

  // paths to output folders, redefined to use `outdir` above
  checkpoints_dir = "${params.outdir}/checkpoints"
  results_dir = "${params.outdir}/results"

  // path to reference files
  ref_json = "${projectDir}/test/stub-refs.json"
  ref_rootdir = 's3://scpca-references'

  // barcode files
  barcode_dir      = "${params.ref_rootdir}/barcodes/10X"
}

